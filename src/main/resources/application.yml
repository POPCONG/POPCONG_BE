spring:
  profiles:
    include: secret

  jpa:
    hibernate:
      ddl-auto: update # 필요시 create
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    open-in-view: false