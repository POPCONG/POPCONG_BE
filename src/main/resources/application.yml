spring:
  profiles:
    include: secret

  jpa:
    hibernate:
      ddl-auto: create # 추후 update로 수정
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    open-in-view: false