spring:
  profiles:
    include: secret

  jpa:
    hibernate:
      ddl-auto: update # 필요시 create
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    open-in-view: false

cloud:
  aws:
    profile: dev
    region: us-east-2     # 버킷 리전
    s3:
      bucket: popcongbucket
